package org.nakii.valmora.module.modifier;

/** Presentation policy only — never carries application semantics (docs/Valmora_Modifier_Framework_Design.docx §6). */
public enum DisplayFormat {
    PREFIX,
    SUFFIX,
    LORE,
    NONE
}
